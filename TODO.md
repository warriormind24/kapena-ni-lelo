# TODO — Kapena Ni Lelo site improvements

- [x] Update `script.js` gallery “Show more” to append incrementally (avoid full re-render).
- [x] Update `script.js` gallery lazy-loading strategy (eager for first row only; lazy for the rest).
- [ ] Update `api/facebook-embed.js` to add server-side caching (in-memory LRU/simple Map) and add request timeout + fallback.
- [x] Update `script.js` Facebook embed rendering to handle timeouts and show a better fallback quickly.
- [ ] Update `api/facebook-embed.js` to add server-side caching (in-memory LRU/simple Map) and add request timeout + fallback.
- [ ] Update `index.html` to add Twitter Card tags and (optional) canonical tag placeholder.
- [ ] Run a quick static sanity check: load in browser, verify gallery, lightbox, news embeds, and FAQ chatbot.



